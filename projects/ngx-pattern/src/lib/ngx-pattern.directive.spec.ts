import { NgxPatternDirective } from './ngx-pattern.directive';

describe('NgxPatternDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxPatternDirective(null);
    expect(directive).toBeTruthy();
  });
});

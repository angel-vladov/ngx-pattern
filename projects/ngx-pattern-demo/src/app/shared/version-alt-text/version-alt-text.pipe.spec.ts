import { VersionAltTextPipe } from './version-alt-text.pipe';

describe('VersionAltTextPipe', () => {
  it('create an instance', () => {
    const pipe = new VersionAltTextPipe();
    expect(pipe).toBeTruthy();
  });
});

import { VersionBadgePipe } from './version-badge.pipe';

describe('VersionBadgePipe', () => {
  it('create an instance', () => {
    const pipe = new VersionBadgePipe();
    expect(pipe).toBeTruthy();
  });
});
